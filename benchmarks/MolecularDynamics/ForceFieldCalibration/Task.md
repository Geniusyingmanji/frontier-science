# ForceFieldCalibration — Fit LJ parameters to match RDF peaks. Oracle: MC simulation of 100 particles in NVT ensemble.

## Your task
```python
def calibrate_forcefield(n_params):
    \"\"\"Return (n_params,) array of design parameters in [-2, 2].\"\"\"
```
## Rules
- Only edit `solution.py`. numpy/scipy only. CPU. Do not read `verification/`.
