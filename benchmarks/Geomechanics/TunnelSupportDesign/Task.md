# TunnelSupportDesign — Optimize rock bolt pattern for tunnel stability. Oracle: 2D plane-strain FEM with Mohr-Coulomb failure criterion.

## Your task
```python
def design_support(n_params):
    \"\"\"Return (n_params,) array of design parameters in [-2, 2].\"\"\"
```
## Rules
- Only edit `solution.py`. numpy/scipy only. CPU. Do not read `verification/`.
