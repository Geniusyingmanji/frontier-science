# NMRSpectrumFitting — decompose spectrum into overlapping Lorentzian peaks

## Your task
```python
def fit_spectrum(x, spectrum):
    \"\"\"Given x (ppm axis) and spectrum (intensity), return dict with
    'centers', 'widths', 'amplitudes' arrays for each detected peak.\"\"\"
```
## Rules
- Only edit `solution.py`. numpy/scipy only. CPU. Do not read `verification/`.
