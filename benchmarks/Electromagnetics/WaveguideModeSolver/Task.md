# WaveguideModeSolver — Optimize waveguide cross-section dimensions. Oracle: solve transcendental eigenvalue equation for TE/TM modes.

## Your task
```python
def design_waveguide(n_params):
    \"\"\"Return (n_params,) array of design parameters in [-2, 2].\"\"\"
```
## Rules
- Only edit `solution.py`. numpy/scipy only. CPU. Do not read `verification/`.
