1) Only edit solution.py. Keep optimize_topology(nelx, nely, volfrac) -> (nely, nelx) array.
2) Mean density <= volfrac (with 5% tolerance). Values in [0.001, 1.0].
3) numpy/scipy only. CPU, <30s. Do not read verification/ or frontier_eval/.
