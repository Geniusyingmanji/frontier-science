# CzochralskiProcess — Crystal pulling speed + rotation optimization. Oracle: 1D heat equation along ingot with moving boundary (Stefan problem).

## Your task
```python
def optimize_czochralski(n_params):
    \"\"\"Return (n_params,) array of design parameters in [-2, 2].\"\"\"
```
## Rules
- Only edit `solution.py`. numpy/scipy only. CPU. Do not read `verification/`.
