# LaserCavityDesign — Optimize Fabry-Perot laser cavity parameters. Oracle: round-trip gain/loss + mode stability (ABCD matrix propagation).

## Your task
```python
def design_cavity(n_params):
    \"\"\"Return (n_params,) array of design parameters in [-2, 2].\"\"\"
```
## Rules
- Only edit `solution.py`. numpy/scipy only. CPU. Do not read `verification/`.
