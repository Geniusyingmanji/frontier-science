import numpy as np


def evaluate(design_reactor):
    """Evaluator for ReactorDesign — to be filled with domain-specific logic."""
    # Placeholder: returns 0 for any input
    return {"combined_score": 0.0, "valid": 1.0, "feasibility_rate": 1.0, "note": "placeholder evaluator"}
