1) Only edit solution.py. Keep pack_circles(n) -> (centers, side).
2) All circles (radius 1) must fit inside [0, side]^2, no overlaps.
3) numpy/stdlib only. CPU, seconds per N. Do not read verification/ or frontier_eval/.
