"""Evaluator for EarthquakeLocalization: Locate earthquake hypocenter from P-wave arrival times."""
import numpy as np

def evaluate(locate_earthquake):
    return {"combined_score": 0.0, "valid": 1.0, "feasibility_rate": 1.0,
             "note": "evaluator stub — replace with domain-specific scoring"}
